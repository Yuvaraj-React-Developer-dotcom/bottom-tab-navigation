//import registrationReducer from '../modules/LoginModule/store/registrationReducer';

import userregistrationdata from '../modules/LoginModule/store/registrationReducer';
const reducers: {
  // Reducer: typeof registrationReducer;
  userregistrationdata: typeof userregistrationdata;
} = {
  // registrationReducer,
  userregistrationdata,
};

export default reducers;
